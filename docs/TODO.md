# 项目待办

- [ ] **本地视觉分析：模型选型、采用来源和检索文本升级。** 详细设计见 [本地视觉分析与向量检索实施计划](local-vision-analysis-plan.md)。建议 Qwen3.5-2B 本机首验、4B 质量对照，单图串行；分析先沿用 v1，权重存在本机缓存，模型清单/分析/采用来源/检索文本/向量存 SQLite；继续使用 E5 多语言向量，新增有 token 预算的文本配方。状态：仅完成研究和设计，尚未下载视觉模型、运行照片分析、迁移数据库或重建向量。真实推理待后续明确确认方案后执行。

- [x] **一个 SQLite 管理多个相册。** 已实现相册与来源目录分离、照片多对多关联、相册重命名及成员操作。移除关联不删除照片或分析。
- [x] **缩略图存入 SQLite 独立表。** schema v3 已实现 BLOB 按需读取，导入、分析、查询及报告均已接通。正式图库 129 张缩略图已迁移核对，原图保持原位置。

- [x] **导入后显示分析状态并提示下一步。** 可选目标相册、分析汇总、分页清单和 Skill 交互规则已实现。仅导入、建相册、查询和报告不调用模型；分析按用户授权范围执行。

本轮验证：66 项测试、Skill 格式校验、12 张合成图的连续扫描及正式图库迁移核对通过；原有 3 条分析保留且有效，新增模型调用为 0。见 改造验收（本地历史文档，未发布）。

以上设计、数据示例和迁移检查见 相册与缩略图存储设计（本地历史文档，未发布），具体步骤见 相册改造实施计划（本地历史文档，未发布）。

- [x] **本地描述向量生成与保存。** schema v4 已实现版本化向量与编码配置、增量生成、状态查询和恢复。按后续确认采用一个本地多语言模型，现有三条中文描述已生成一套向量；未调用视觉模型或生成双语描述。
- [x] **语义搜索与搜索结果加入相册。** 已实现中英文查询、相册/图库范围筛选、精确余弦排序、覆盖统计、可选择的结果快照，以及按快照中的明确照片 ID 加入相册。搜索不自动生成缺失向量或分析。

本轮正式图库仍为 129 张、3 条分析，其中 3 张可搜索、126 张缺少有效分析。六条中英文真实查询及十二条标注文本查询通过；完整数据保留、迁移备份和离线运行已验证。详见 本地搜索验收（本地历史文档，未发布）。

详细流程与查询示例见 本地语义搜索设计（本地历史文档，未发布），分阶段实施与验收见 本地向量与搜索实施计划（本地历史文档，未发布）。

已确认优先级：search 是下一项核心功能，先打通搜索与“将选中的结果加入相册”。cluster 移出第一版必做范围，暂缓为后续自动发现潜在系列的可选能力；原有草案（本地历史文档，未发布） 保留作历史参考。导入仍不会自动触发模型分析。

- [x] **分析前生成调用方案，由用户确认后执行。** 已实现非敏感配置保存、输入/缓存检查、推荐与确认摘要、即时请求队列与限流恢复、OpenAI Batch 提交/查询/回收/取消/清理，以及实验性多图即时请求。即时与 Batch 是两种模式，每请求照片数与并发数是即时模式的设置。新增请求级已知用量、未知用量标记、状态查询与方案 HTML。正式数据库已升级至 v5；原有 129 张照片、3 条分析和3条向量逐表核对保持不变，原 Codex 配置三条缓存可复用。本轮仅离线测试和正式数据预检，没有新增照片分析或远程请求。见 改造验收（本地历史文档，未发布） 和 实施计划（本地历史文档，未发布）。

- [ ] **经用户确认后进行最小真实 API 验证。** 当前离线验证不代表真实账户的模型访问、Batch 周期、多图质量或节省成本已经验证。多图默认保持实验选项；价格/输入 token 缺少依据时明确显示未知。暂不调用模型。

- [ ] **让 Smart Albums Skill 使用宿主的模型能力，减少额外凭证配置。**

状态：用户决定暂缓，后续再解决。

期望：其他用户在 ChatGPT 或 Codex 中安装并使用 Skill 时，优先由他们当前使用的宿主模型读取照片，Skill 代码负责校验、保存和缓存结果；不要求用户向 Skill 提供登录令牌，不在安装包中分发任何人的凭证。独立 API 接法可作为可选方式保留。

后续需要确认并完成：

1. 分别验证 ChatGPT 与 Codex 环境中可用的图片输入、脚本执行、图库访问和持久化能力，明确支持范围。
2. 设计宿主模型与保存代码之间的结构化结果交接，保留照片版本检查、缓存和逐张错误处理。
3. 评估 Codex 命令行复用用户已有登录的方式；它只适用于相应的本机环境，不能直接当作通用 ChatGPT 方案。
4. 验证新用户的安装和使用流程：环境支持时直接使用自己的账号；不支持时给出清楚的配置路径。

当前事实：独立 OpenAI Responses 接法已实现，并通过离线测试。本机确认 Codex 已通过 ChatGPT 登录。随后为了三张照片的计时及 token 测量，已增加可选的本机 Codex CLI 接法，由 Codex 自己管理登录；未读取或复制登录令牌。这不等于已完成跨 ChatGPT/Codex 宿主的通用适配。

本轮授权：用户随后明确要求从现有图库任选三张进行真实分析，测量每张耗时和 token。仅扩展到这三张，不自动处理其余照片。通用宿主适配仍暂缓。
